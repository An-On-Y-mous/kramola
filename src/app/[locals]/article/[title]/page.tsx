import { NextPage } from "next";

interface Props {}

const ArticleLocale: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1>Other language Articles</h1>
    </div>
  );
};

export default ArticleLocale;
